import { Bebas_Neue, Roboto } from "next/font/google";

export const bebas_neue = Bebas_Neue({
  weight: "400",
  subsets: ["latin"],
});

export const roboto = Roboto({
  weight: "100",
  subsets: ["latin"],
});

export const roboto_bolder = Roboto({
  weight: "900",
  subsets: ["latin"],
});